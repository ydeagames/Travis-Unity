language: objective-c

branches:
  only:
    - alfa
    - beta
    - stable

os: osx
osx_image: xcode8.3

rvm:
  - 2.2

addons:
  - sonarcloud:
    organization: "ydeagames-github"

env:
  global:
    - ANDROID_SDK_ROOT=/usr/local/share/android-sdk
    - ANDROID_NDK_HOME=/usr/local/share/android-ndk

cache:
  - directories:
    # Homebrew    
    - $HOME/Library/Caches/Homebrew
    # Android
    - $ANDROID_SDK_ROOT
    - $ANDROID_NDK_HOME
    # Unity
    - ./Unity
    - $HOME/Library/Caches/com.unity3d.UnityEditor/GiCache
  - timeout: 1000

before_install:
  # travis encrypt-file
  - openssl aes-256-cbc -K $encrypted_071e2bb50fdb_key -iv $encrypted_071e2bb50fdb_iv -in ./Travis/Secrets/secrets.tar.enc -out ./Travis/Secrets/secrets.tar -d
  - tar xvf ./Travis/Secrets/secrets.tar -C ./Travis/Secrets
  # xcpretty and fomrmatter
  - gem install xcpretty-travis-formatter --no-document

install:
  - brew install tree
  - sh ./Travis/install.sh
#  - sh ./Travis/install_android.sh

before_script:
  # Create default keychain on VM
  # http://docs.travis-ci.com/user/common-build-problems/#Mac%3A-Code-Signing-Errors
  - security create-keychain -p travis ios-build.keychain
  - security default-keychain -s ios-build.keychain
  - security unlock-keychain -p travis ios-build.keychain
  - security set-keychain-settings -t 3600 -l ~/Library/Keychains/ios-build.keychain
  # Add certs to keychain
  - security import ./Travis/Secrets/travis.p12 -k ~/Library/Keychains/ios-build.keychain -P ${KEY_PASSWORD} -T /usr/bin/codesign
  # save profile
  - mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
  - cp "./Travis/Secrets/profiles/travis.mobileprovision" ~/Library/MobileDevice/Provisioning\ Profiles/

script:
  - sonar-scanner
  - tree -L 3
  - sh ./Travis/build.sh
  - tree -L 3
  # iOS
  - cd ./Build/ios
  - xcodebuild -project ${APP_NAME}.xcodeproj -target ${APP_NAME} -sdk iphoneos -configuration Release CODE_SIGN_IDENTITY="${DEVELOPER_NAME}" archive -archivePath ./build/${APP_NAME}.xcarchive | xcpretty -f `xcpretty-travis-formatter` && exit ${PIPESTATUS[0]}
  - xcodebuild -exportArchive -exportFormat IPA -archivePath ./build/${APP_NAME}.xcarchive -exportPath ./build.ipa -exportProvisioningProfile "${PROFILE_NAME}" | xcpretty -f `xcpretty-travis-formatter` && exit ${PIPESTATUS[0]}
  - cd ../../

after_success:
  - sh ./Travis/tag.sh
  # Upload .ipa file to deploygate
  - curl -F "file=@./Build/ios/build.ipa" -F "token=${DEPLOYGATE_API_TOKEN}" https://deploygate.com/api/users/${DEPLOYGATE_USER}/apps

after_script:
  - security delete-keychain ios-build.keychain
  - rm -f "~/Library/MobileDevice/Provisioning Profiles/${PROFILE_NAME}.mobileprovision"

deploy:
  - provider: releases
    api_key: $GIT_TOKEN
    file: 
      # - ./Build/windows.zip
      # - ./Build/linux.tar.gz
      # - ./Build/android.apk
      # - ./Build/osx.dmg
      - ./Build/ios/build.ipa
    name: $TRAVIS_BRANCH $(date +'%Y.%m.%d %H:%M')
    skip_cleanup: true
    on:
      all_branches: true

#  - provider: pages
#    skip-cleanup: true
#    github-token: $GIT_TOKEN
#    local-dir: ./Build/webgl/
#    keep-history: true
#    on:
#      branch: stable

notifications:
  email: true